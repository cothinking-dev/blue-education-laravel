domains:
    - blue-education-laravel
php_version: "8.5"
framework: laravel
framework_version: "12"
services:
    - sqlite
workers:
    - queue
custom_workers:
    vite:
        label: Vite Dev Server
        command: npm run dev
        restart: always
